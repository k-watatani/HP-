---
import Layout from '../layouts/Layout.astro';

const title = "Inside Heart株式会社 | ホーム";
const description = "Inside Heart（インサイドハート）の公式サイト。一度きりの人生に、挑戦のきっかけを。ITの力でビジネスをなめらかにします。";
---

<Layout title={title} description={description}>
  <!-- Hero -->
  <section class="hero">
    <div class="hero-bg-wrapper">
      <canvas id="hero-canvas" class="hero-canvas"></canvas>
      <div class="hero-blur-overlay"></div>
    </div>
    <div class="hero-light-layer">
      <div class="light-blob light-blue"></div>
      <div class="light-blob light-orange"></div>
    </div>
    <div class="container hero-inner">
      <div class="hero-grid">
        <div class="hero-content reveal">
          <span class="category-label">Inside Heart Co., Ltd.</span>
          <h1><span class="nowrap">一度きりの人生に、</span><br/><span class="nowrap">挑戦のきっかけを。</span></h1>
          <p class="hero-sub">ITと人の力を融合させ、企業の成長と組織の活性化を支援する。<br/>本質的な価値に向き合い、パートナーシップを築きます。</p>
          <div class="hero-actions">
            <a href="/contact" class="btn btn-primary">contact</a>
            <a href="/company" class="btn btn-secondary">about us</a>
          </div>
        </div>
        <div class="hero-visual reveal reveal-delay-2">
          <div class="visual-container">
            <div class="floating-node node-1"></div>
            <div class="floating-node node-2"></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services -->
  <section class="section" id="services">
    <div class="container">
      <div class="section-header reveal">
        <span class="section-num">01 / Our Services</span>
        <h2>事業紹介</h2>
      </div>
      <div class="cards-row reveal">
        <div class="service-item">
          <div class="card-title">Miroku</div>
          <div class="card-sub">Infrastructure for Motivation</div>
          <p class="card-desc">
            心理学に基づいた可視化と称賛の仕組みで、組織のポテンシャルを最大化する自社開発クラウドインフラ。
          </p>
          <a href="/miroku" class="card-link">View project &rarr;</a>
        </div>
        <div class="service-item">
          <div class="card-title">営業支援</div>
          <div class="card-sub">Strategy & Execution</div>
          <p class="card-desc">
            戦略構築から現場での並走支援まで。営業活動を科学し、成果への最短距離をエンジニアリングします。
          </p>
          <a href="/service" class="card-link">View project &rarr;</a>
        </div>
        <div class="service-item">
          <div class="card-title">システム・AI開発</div>
          <div class="card-sub">Technology Solutions</div>
          <p class="card-desc">
            最先端の技術を活用したシステム構築。AI・受託開発でビジネスプロセスを「なめらか」にします。
          </p>
          <a href="/service" class="card-link">View project &rarr;</a>
        </div>
        <div class="service-item">
          <div class="card-title">コンサル・Webマーケ</div>
          <div class="card-sub">Strategy & Growth</div>
          <p class="card-desc">
            データに基づいた戦略立案。デジタル領域の集客から成約までを最適化し、事業成長を加速させます。
          </p>
          <a href="/service" class="card-link">View project &rarr;</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Philosophy -->
  <section class="section bg-soft" id="philosophy">
    <div class="container">
      <div class="editorial-block reveal">
      <div class="section-header reveal">
          <span class="section-num">02 / Mission</span>
          <h2 class="block-title">理念</h2>
        </div>
        <div class="philosophy-content">
          <h3 class="philosophy-main">大切な人の、大切な人を大切にする</h3>
          <p class="block-sub">私たちは関わったすべての人々の、物心両面の豊かさと幸福を追求します。</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Values -->
  <section class="section" id="values">
    <div class="container">
      <div class="editorial-block reveal">
      <div class="section-header reveal">
          <span class="section-num">03 / Our Values</span>
          <h2 class="block-title">大切にする価値観</h2>
        </div>
        <div class="block-content">
          <h3 class="values-text">
            愛 / 感謝 / 誠実 / 素直 / 勉強熱心 / 前向行動
          </h3>
          <p class="block-sub">私たちはこれらの価値観を大切にして行動します。</p>
        </div>
      </div>
    </div>
  </section>

  <!-- News -->
  <section class="section bg-soft" id="news">
    <div class="container">
      <div class="section-header reveal">
        <span class="section-num">04 / News</span>
        <h2>お知らせ</h2>
      </div>
      <div class="news-list reveal">
        <div class="news-item">
          <span class="news-date">2026.03.01</span>
          <span class="news-text">Inside Heart Co., Ltd. を設立いたしました。</span>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Hero Refinement */
  .hero {
    position: relative;
    padding: 180px 0;
    overflow: hidden;
    background-color: var(--surface);
    display: flex;
    align-items: center;
    min-height: 90vh;
  }

  .category-label {
    display: block;
    font-family: var(--font-latin);
    font-size: 14px;
    font-weight: 700;
    color: var(--accent);
    margin-bottom: 1.5rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .hero-bg-wrapper {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
  }

  .hero-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    opacity: 0.6;
  }

  @keyframes slowZoom {
    0% { transform: scale(1.1); }
    100% { transform: scale(1.25); }
  }

  .hero-blur-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 50% 50%, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0.9) 100%);
    z-index: 1;
  }

  /* Floating Light Particles - Robust Implementation */
  .hero-light-layer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .light-blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    mix-blend-mode: screen;
    opacity: 0.3;
  }

  .light-blue {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, var(--accent) 0%, transparent 70%);
    top: -10%;
    left: -10%;
    animation: floatBlue 25s ease-in-out infinite alternate;
  }

  .light-orange {
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, #ff9d00 0%, transparent 70%);
    bottom: -10%;
    right: -10%;
    animation: floatOrange 20s ease-in-out infinite alternate-reverse;
  }

  @keyframes floatBlue {
    0% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(15%, 15%) scale(1.1); }
    100% { transform: translate(-5%, 25%) scale(1); }
  }

  @keyframes floatOrange {
    0% { transform: translate(0, 0) scale(1); }
    50% { transform: translate(-15%, -15%) scale(1.2); }
    100% { transform: translate(5%, -5%) scale(1.1); }
  }

  @media (max-width: 992px) {
    .hero-grid {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .hero-visual {
      display: none;
    }
    
    .hero-content h1 {
      margin: 0 auto 2rem;
    }
    
    .hero-bg-overlay {
      background: rgba(255, 255, 255, 0.9);
    }
  }

  .hero-inner {
    position: relative;
    z-index: 1;
  }

  .hero-content h1 {
    margin-bottom: 2rem;
    max-width: 900px;
  }

  /* Section Header */
  .section-header {
    margin-bottom: 4rem;
  }

  .section-num {
    display: block;
    font-family: var(--font-latin);
    font-size: 13px;
    font-weight: 700;
    color: var(--muted);
    margin-bottom: 1rem;
    letter-spacing: 0.1em;
  }

  .pc-only {
    display: block;
  }

  @media (max-width: 768px) {
    .pc-only {
      display: none;
    }
  }

  /* Editorial Block (Sync for 02 & 03) */
  .editorial-block {
    display: flex;
    flex-direction: column;
    gap: 40px;
    max-width: 1000px;
  }

  .block-title {
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 800;
    margin: 0;
    color: var(--ink);
    line-height: 1.2;
    letter-spacing: -0.01em;
  }

  .block-content {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .block-main {
    font-size: clamp(1.25rem, 3.5vw, 2rem);
    font-weight: 800;
    line-height: 1.3;
    color: var(--ink);
    margin: 0;
  }

  .block-sub {
    font-size: clamp(0.9rem, 4vw, 1.2rem);
    line-height: 1.6;
    color: var(--muted);
    font-weight: 500;
    margin: 0;
  }

  /* News list refined */
  .news-list {
    border-top: 1px solid var(--line);
  }

  .news-item {
    display: flex;
    padding: 30px 0;
    border-bottom: 1px solid var(--line);
    gap: 40px;
    align-items: baseline;
  }

  .news-date {
    font-family: var(--font-latin);
    font-weight: 700;
    font-size: 14px;
    color: var(--muted);
    min-width: 100px;
  }

  @media (max-width: 768px) {
    .hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-content h1 {
      font-size: clamp(2rem, 10vw, 3.5rem);
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-spacer {
      display: none;
    }

    .news-item {
      flex-direction: column;
      gap: 8px;
      padding: 24px 0;
    }

    .news-date {
      min-width: auto;
    }

    .editorial-block {
      gap: 24px;
    }
    
    .section-header {
      margin-bottom: 2.5rem;
    }
  }
  /* Values specific fine-tuning */
  .values-text {
    font-size: clamp(1.15rem, 3vw, 1.6rem);
    font-weight: 800;
    line-height: 1.3;
    color: var(--ink);
    margin: 0;
    letter-spacing: 0.1em;
  }

  /* Philosophy specific styles */
  .philosophy-content {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .philosophy-main {
    font-size: clamp(1.25rem, 3.5vw, 2rem);
    font-weight: 800;
    line-height: 1.3;
    color: var(--ink);
    margin: 0;
  }

</style>

<script>
  const canvas = document.getElementById('hero-canvas') as HTMLCanvasElement;
  if (canvas) {
    const ctx = canvas.getContext('2d');
    if (ctx) {
      let width: number, height: number, particles: any[];

      const init = () => {
        width = canvas.width = window.innerWidth;
        height = canvas.height = window.innerHeight;
        particles = [];
        const count = Math.min(Math.floor(width / 10), 100);
        for (let i = 0; i < count; i++) {
          particles.push({
            x: Math.random() * width,
            y: Math.random() * height,
            vx: (Math.random() - 0.5) * 0.5,
            vy: (Math.random() - 0.5) * 0.5,
            size: Math.random() * 2.5 + 1.2
          });
        }
      };

      const animate = () => {
        ctx.clearRect(0, 0, width, height);
        particles.forEach((p, i) => {
          p.x += p.vx;
          p.y += p.vy;
          if (p.x < 0 || p.x > width) p.vx *= -1;
          if (p.y < 0 || p.y > height) p.vy *= -1;

          ctx.beginPath();
          ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
          ctx.fillStyle = 'rgba(7, 68, 150, 0.6)';
          ctx.fill();

          for (let j = i + 1; j < particles.length; j++) {
            const p2 = particles[j];
            const dist = Math.hypot(p.x - p2.x, p.y - p2.y);
            if (dist < 200) {
              ctx.beginPath();
              ctx.moveTo(p.x, p.y);
              ctx.lineTo(p2.x, p2.y);
              ctx.strokeStyle = `rgba(7, 68, 150, ${0.3 * (1 - dist / 200)})`;
              ctx.lineWidth = 0.8;
              ctx.stroke();
            }
          }
        });
        requestAnimationFrame(animate);
      };

      window.addEventListener('resize', init);
      init();
      animate();
    }
  }
</script>
