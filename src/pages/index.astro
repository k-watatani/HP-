---
import Layout from '../layouts/Layout.astro';

const title = "Inside Heart株式会社 | ホーム";
const description = "インサイドハート株式会社の公式サイト。大切な人の、大切な人を大切にする。ITと人の力で企業の成長を支援します。";
---

<Layout title={title} description={description}>
  <!-- Hero -->
  <section class="hero">
    <div class="hero-bg-wrapper">
      <div class="hero-image" style="background-image: url('https://images.unsplash.com/photo-1451187580459-43490279c0fa?auto=format&fit=crop&q=80&w=2072');"></div>
      <div class="hero-curves">
        <svg viewBox="0 0 1000 1000" preserveAspectRatio="none">
          <path class="curve curve-1" d="M0,500 C200,400 300,600 500,500 C700,400 800,600 1000,500 V1000 H0 Z"></path>
          <path class="curve curve-2" d="M0,600 C250,500 350,700 550,600 C750,500 850,700 1050,600 V1000 H0 Z"></path>
        </svg>
      </div>
      <div class="hero-bg-overlay"></div>
    </div>
    <div class="container hero-inner">
      <div class="hero-grid">
        <div class="hero-content reveal">
          <span class="category-label">Inside Heart Co., Ltd.</span>
          <h1>大切な人の、<br/>大切な人を大切にする</h1>
          <p class="hero-sub">ITと人の力を融合させ、企業の成長と組織の活性化を支援する。<br/>本質的な価値に向き合い、パートナーシップを築きます。</p>
          <div class="hero-actions">
            <a href="/contact" class="btn btn-primary">contact</a>
            <a href="/company" class="btn btn-secondary">about us</a>
          </div>
        </div>
        <div class="hero-visual reveal reveal-delay-2">
          <div class="visual-card">
            <div class="visual-inner">
              <span class="visual-tag">Technology & People</span>
              <div class="visual-dots"></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services -->
  <section class="section" id="services">
    <div class="container">
      <div class="section-header reveal">
        <span class="section-num">01 / Our Services</span>
        <h2>事業紹介</h2>
      </div>
      <div class="cards-grid">
        <div class="card reveal reveal-delay-1">
          <div class="card-visual">
            <div class="visual-placeholder">Operation Infrastructure</div>
          </div>
          <div class="card-title">Miroku</div>
          <div class="card-sub">Infrastructure for Motivation</div>
          <p class="card-desc">
            心理学に基づいた可視化と称賛の仕組みで、組織のポテンシャルを最大化するインフラ。
          </p>
          <a href="/miroku" class="card-link">View project &rarr;</a>
        </div>
        <div class="card reveal reveal-delay-2">
          <div class="card-visual">
            <div class="visual-placeholder">Sales Engineering</div>
          </div>
          <div class="card-title">営業支援</div>
          <div class="card-sub">Strategy & Execution</div>
          <p class="card-desc">
            戦略構築から現場での並走支援まで。営業活動を科学し、成果への最短距離をエンジニアリングします。
          </p>
          <a href="/service" class="card-link">View project &rarr;</a>
        </div>
        <div class="card reveal reveal-delay-3">
          <div class="card-visual">
            <div class="visual-placeholder">Systems & AI</div>
          </div>
          <div class="card-title">システム・AI開発</div>
          <div class="card-sub">Technology Solutions</div>
          <p class="card-desc">
            最先端の技術を活用したシステム構築。ビジネスプロセスをテクノロジーで「なめらか」にします。
          </p>
          <a href="/service" class="card-link">View project &rarr;</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Philosophy -->
  <section class="section bg-soft" id="philosophy">
    <div class="container">
      <div class="philosophy-container reveal">
        <div class="section-header">
          <span class="section-num">02 / Mission</span>
          <h2>理念</h2>
        </div>
        <div class="philosophy-content">
          <h3 class="philosophy-main">大切な人の、<br class="pc-only" />大切な人を大切にする</h3>
        </div>
      </div>
    </div>
  </section>

  <!-- News -->
  <section class="section" id="news">
    <div class="container">
      <div class="section-header reveal">
        <span class="section-num">03 / News</span>
        <h2>お知らせ</h2>
      </div>
      <div class="news-list reveal">
        <div class="news-item">
          <span class="news-date">2026.03.01</span>
          <span class="news-text">Inside Heart Co., Ltd. を設立いたしました。</span>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Hero Refinement */
  .hero {
    position: relative;
    padding: 180px 0;
    overflow: hidden;
    background-color: var(--surface);
    display: flex;
    align-items: center;
    min-height: 90vh;
  }

  .category-label {
    display: block;
    font-family: var(--font-latin);
    font-size: 14px;
    font-weight: 700;
    color: var(--accent);
    margin-bottom: 1.5rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .hero-bg-wrapper {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
  }

  .hero-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-size: cover;
    background-position: center;
    filter: blur(10px);
    opacity: 0.2;
    transform: scale(1.05);
  }

  .hero-curves {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0.3;
  }

  /* Grid for Hero */
  .hero-grid {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 40px;
    align-items: center;
  }

  .hero-visual {
    position: relative;
    display: flex;
    justify-content: flex-end;
  }

  .visual-card {
    width: 100%;
    max-width: 400px;
    aspect-ratio: 1 / 1;
    background: rgba(240, 240, 240, 0.1);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.5);
    padding: 1px;
    position: relative;
    box-shadow: 0 40px 100px rgba(0, 0, 0, 0.05);
  }

  .visual-inner {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #ffffff 0%, #f0f0f0 100%);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  .visual-tag {
    font-family: var(--font-latin);
    font-size: 11px;
    font-weight: 700;
    color: var(--accent);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    position: absolute;
    top: 40px;
  }

  .visual-dots {
    width: 120px;
    height: 120px;
    background-image: radial-gradient(var(--line) 2px, transparent 2px);
    background-size: 20px 20px;
    opacity: 0.5;
  }

  .hero-bg-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, #ffffff 30%, rgba(255, 255, 255, 0.7) 100%);
  }

  @media (max-width: 992px) {
    .hero-grid {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .hero-visual {
      display: none;
    }
    
    .hero-content h1 {
      margin: 0 auto 2rem;
    }
    
    .hero-bg-overlay {
      background: rgba(255, 255, 255, 0.9);
    }
  }

  .hero-inner {
    position: relative;
    z-index: 1;
  }

  .hero-content h1 {
    margin-bottom: 2rem;
    max-width: 900px;
  }

  /* Section Header */
  .section-header {
    margin-bottom: 4rem;
  }

  .section-num {
    display: block;
    font-family: var(--font-latin);
    font-size: 13px;
    font-weight: 700;
    color: var(--muted);
    margin-bottom: 1rem;
    letter-spacing: 0.1em;
  }

  /* Card Grid adjustments */
  .cards-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px; /* PKSHA-like thin grid line effect */
    background-color: var(--line);
  }

  .card {
    background: white;
    padding: 60px 40px;
    transition: background 0.4s ease;
    border-radius: 0;
    box-shadow: none;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .card-visual {
    margin-bottom: 10px;
  }

  .visual-placeholder {
    background-color: var(--support);
    aspect-ratio: 16 / 9;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--muted);
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    font-weight: 700;
  }

  .pc-only {
    display: block;
  }

  @media (max-width: 768px) {
    .pc-only {
      display: none;
    }
    
    .cards-grid {
      grid-template-columns: 1fr;
    }
  }

  /* News list refined */
  .news-list {
    border-top: 1px solid var(--line);
  }

  .news-item {
    display: flex;
    padding: 30px 0;
    border-bottom: 1px solid var(--line);
    gap: 40px;
    align-items: baseline;
  }

  .news-date {
    font-family: var(--font-latin);
    font-weight: 700;
    font-size: 14px;
    color: var(--muted);
    min-width: 100px;
  }

  @media (max-width: 768px) {
    .hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-content h1 {
      font-size: 32px;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-spacer {
      display: none;
    }

    .philosophy-container {
      padding: 40px 24px;
    }

    .mission-text {
      font-size: 24px;
    }

    .news-item {
      flex-direction: column;
      align-items: flex-start;
      gap: 4px;
    }
  }
</style>
